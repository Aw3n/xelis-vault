hook constructor() -> u64 {
    Storage::new().store("a", 1u64);
    return 0
}

entry test_mint(xusd: Hash, to: Address, amount: u64) -> u64 {
    let xc: Contract = Contract::new(xusd).expect("xcnf");
    let _: u64 = xc.call(6u16, [to, amount], {});
    return 0
}
