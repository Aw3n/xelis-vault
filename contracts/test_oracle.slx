hook constructor() -> u64 {
    Storage::new().store("a", 1u64);
    return 0
}

entry call_oracle(oracle: Hash) -> u64 {
    let oc: Contract = Contract::new(oracle).expect("ocnf");
    let price: u64 = oc.call(3u16, [Hash::zero()], {});
    Storage::new().store("p", price);
    return 0
}
